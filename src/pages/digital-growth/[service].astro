---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import QuoteForm from '../../components/QuoteForm.astro';
import { digitalGrowthServices } from '../../data/digitalGrowth.js';

export async function getStaticPaths() {
  return digitalGrowthServices.map(s => ({ params: { service: s.id }, props: { service: s } }));
}
const { service: svc } = Astro.props;
const related = digitalGrowthServices.filter(s => s.id !== svc.id);

// Determine which tool/platform list to render
const toolList = svc.brands ?? svc.tech ?? svc.platforms ?? [];

const LOGO_TOKEN = 'pk_QjVOM58vT--W_C9INwBCvg';
---

<BaseLayout
  title={`${svc.name} Services | Crewtec`}
  description={svc.metaDesc}
  canonical={`/digital-growth/${svc.id}`}
>
  <Header backLink={true} />
  <main>

    <!-- ═══════════════════════════════════════════════
         HERO — navy bg, dot-grid
    ═══════════════════════════════════════════════ -->
    <section class="ds-hero">
      <div class="ds-hero-inner">
        <!-- Breadcrumb -->
        <nav class="ds-breadcrumb" aria-label="Breadcrumb">
          <a href="/">Home</a>
          <span>/</span>
          <a href="/digital-growth">Digital Growth</a>
          <span>/</span>
          <span>{svc.name}</span>
        </nav>

        <!-- Service badge -->
        <span class="ds-badge">Service {svc.number}</span>

        <h1 class="ds-hero-h1">{svc.name}</h1>
        <p class="ds-hero-desc">{svc.fullDesc}</p>

        <!-- Results strip -->
        <div class="ds-results-strip">
          {svc.results.map(r => (
            <div class="ds-result-item">
              <span class="ds-result-val">{r.value}</span>
              <span class="ds-result-lbl">{r.label}</span>
            </div>
          ))}
        </div>

        <a href="#quote" class="ds-hero-cta">Get Started →</a>
      </div>
    </section>

    <!-- ═══════════════════════════════════════════════
         TWO-COL BODY
    ═══════════════════════════════════════════════ -->
    <div class="ds-body">
      <div class="ds-body-inner">

        <!-- ── LEFT COLUMN ──────────────────────── -->
        <div class="ds-content">

          <!-- Section 1: Process -->
          <section class="ds-section">
            <h2 class="ds-section-h2">Our {svc.name} Process</h2>
            <div class="ds-timeline">
              {svc.process.map((step, i) => (
                <div class="ds-timeline-item">
                  <div class="ds-timeline-left">
                    <div class="ds-timeline-num">{String(i + 1).padStart(2, '0')}</div>
                    {i < svc.process.length - 1 && <div class="ds-timeline-line"></div>}
                  </div>
                  <div class="ds-timeline-content">
                    <h3 class="ds-timeline-title">{step.title}</h3>
                    <p class="ds-timeline-desc">{step.desc}</p>
                  </div>
                </div>
              ))}
            </div>
          </section>

          <!-- Section 2: Features -->
          <section class="ds-section">
            <h2 class="ds-section-h2">What's Included</h2>
            <div class="ds-features-grid">
              {svc.features.map(f => (
                <div class="ds-feature-card">
                  <span class="ds-feat-check">✓</span>
                  <span class="ds-feat-text">{f}</span>
                </div>
              ))}
            </div>
          </section>

          <!-- Section 3: Tools & Technology -->
          {toolList.length > 0 && (
            <section class="ds-section">
              <h2 class="ds-section-h2">Tools We Use</h2>
              <div class="ds-tools-grid">
                {toolList.map(tool => (
                  <div class="ds-tool-card">
                    <div class="ds-tool-logo-wrap">
                      <img
                        src={`https://img.logo.dev/${tool.domain}?token=${LOGO_TOKEN}&size=80&format=webp`}
                        alt={`${tool.name} logo`}
                        width="48"
                        height="48"
                        loading="lazy"
                        onerror={`this.style.display='none';this.nextElementSibling.style.display='flex'`}
                      />
                      <span
                        class="ds-tool-fallback"
                        style="display:none;width:48px;height:48px;background:rgba(0,180,160,0.12);color:#00B4A0;border-radius:10px;align-items:center;justify-content:center;font-size:0.75rem;font-weight:700;"
                      >
                        {tool.name.slice(0, 2).toUpperCase()}
                      </span>
                    </div>
                    <div class="ds-tool-info">
                      <span class="ds-tool-name">{tool.name}</span>
                      <span class="ds-tool-type">{tool.type}</span>
                    </div>
                  </div>
                ))}
              </div>
            </section>
          )}

          <!-- Section 4: Use Cases -->
          <section class="ds-section">
            <h2 class="ds-section-h2">What We Help You Achieve</h2>
            <div class="ds-usecases">
              {svc.useCases.map(uc => (
                <div class="ds-usecase-card">
                  <span class="ds-uc-check">✓</span>
                  <span class="ds-uc-text">{uc}</span>
                </div>
              ))}
            </div>
          </section>

          <!-- Section 5: Related Services -->
          <section class="ds-section">
            <h2 class="ds-section-h2">Related Services</h2>
            <div class="ds-related-grid">
              {related.map(r => (
                <a href={`/digital-growth/${r.id}`} class="ds-related-card">
                  <span class="ds-related-icon">{r.icon}</span>
                  <div class="ds-related-info">
                    <span class="ds-related-name">{r.name}</span>
                    <span class="ds-related-desc">{r.shortDesc}</span>
                  </div>
                  <span class="ds-related-arrow">→</span>
                </a>
              ))}
            </div>
          </section>

        </div>

        <!-- ── RIGHT COLUMN (sticky) ─────────────── -->
        <aside class="ds-sidebar" id="quote">
          <div class="ds-sidebar-sticky">
            <div class="ds-sidebar-banner">
              <p>Interested in <strong>{svc.name}</strong>? Our specialists are ready to help.</p>
            </div>
            <QuoteForm prefill={svc.id} />
            <div class="ds-contact-strip">
              <a href="tel:+919986437648">📞 +91 99864 37648</a>
              <a href="mailto:sales@crewtec.in">✉️ sales@crewtec.in</a>
            </div>
          </div>
        </aside>

      </div>
    </div>

  </main>
  <Footer />
</BaseLayout>

<style>
/* ── HERO ─────────────────────────────────────── */
.ds-hero {
  background: #0A1628;
  padding: 64px 0 56px;
  position: relative;
  overflow: hidden;
}
.ds-hero::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image: radial-gradient(circle, rgba(0,180,160,0.1) 1px, transparent 1px);
  background-size: 40px 40px;
  pointer-events: none;
}
.ds-hero-inner {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 24px;
  position: relative;
  z-index: 1;
}
.ds-breadcrumb {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 0.78rem;
  color: rgba(255,255,255,0.4);
  margin-bottom: 20px;
  flex-wrap: wrap;
}
.ds-breadcrumb a { color: rgba(255,255,255,0.4); transition: color 0.2s; }
.ds-breadcrumb a:hover { color: #00B4A0; }
.ds-breadcrumb span:last-child { color: rgba(255,255,255,0.65); }

.ds-badge {
  display: inline-block;
  font-size: 0.7rem;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: #00B4A0;
  background: rgba(0,180,160,0.12);
  border: 1px solid rgba(0,180,160,0.25);
  padding: 4px 12px;
  border-radius: 100px;
  margin-bottom: 16px;
}

.ds-hero-h1 {
  font-family: 'Syne', sans-serif;
  font-size: clamp(2rem, 4.5vw, 3rem);
  font-weight: 800;
  color: #fff;
  line-height: 1.15;
  margin-bottom: 16px;
  max-width: 720px;
}
.ds-hero-desc {
  font-size: 1rem;
  color: rgba(255,255,255,0.6);
  max-width: 620px;
  line-height: 1.7;
  margin-bottom: 32px;
}
.ds-results-strip {
  display: flex;
  gap: 40px;
  flex-wrap: wrap;
  margin-bottom: 32px;
  padding: 20px 24px;
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 12px;
  width: fit-content;
}
.ds-result-item { text-align: center; }
.ds-result-val {
  display: block;
  font-family: 'Syne', sans-serif;
  font-size: 1.6rem;
  font-weight: 800;
  color: #00B4A0;
}
.ds-result-lbl {
  display: block;
  font-size: 0.72rem;
  color: rgba(255,255,255,0.5);
  margin-top: 4px;
  max-width: 120px;
  line-height: 1.4;
}
.ds-hero-cta {
  display: inline-flex;
  align-items: center;
  padding: 14px 28px;
  background: #00B4A0;
  color: #fff;
  border-radius: 10px;
  font-weight: 700;
  font-size: 0.95rem;
  box-shadow: 0 4px 20px rgba(0,180,160,0.4);
  transition: background 0.2s, transform 0.2s;
}
.ds-hero-cta:hover { background: #009d8c; transform: translateY(-2px); }

/* ── TWO-COL BODY ─────────────────────────────── */
.ds-body {
  background: #F5F9FF;
  padding: 56px 0 80px;
}
.ds-body-inner {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 24px;
  display: grid;
  grid-template-columns: 1fr 380px;
  gap: 40px;
  align-items: start;
}
@media (max-width: 1024px) {
  .ds-body-inner { grid-template-columns: 1fr; }
  .ds-sidebar-sticky { position: static !important; }
}

/* ── SECTIONS ─────────────────────────────────── */
.ds-section {
  margin-bottom: 48px;
}
.ds-section-h2 {
  font-family: 'Syne', sans-serif;
  font-size: 1.25rem;
  font-weight: 700;
  color: #0A1628;
  margin-bottom: 20px;
  padding-bottom: 12px;
  border-bottom: 2px solid #e8eef5;
}

/* ── PROCESS TIMELINE ─────────────────────────── */
.ds-timeline {
  display: flex;
  flex-direction: column;
  gap: 0;
}
.ds-timeline-item {
  display: flex;
  gap: 20px;
  align-items: flex-start;
}
.ds-timeline-left {
  display: flex;
  flex-direction: column;
  align-items: center;
  flex-shrink: 0;
}
.ds-timeline-num {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: #00B4A0;
  color: #fff;
  font-family: 'Syne', sans-serif;
  font-size: 0.75rem;
  font-weight: 800;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.ds-timeline-line {
  width: 2px;
  flex: 1;
  min-height: 28px;
  background: #d0dce8;
  margin: 6px 0;
}
.ds-timeline-content {
  background: #fff;
  border: 1.5px solid #e8eef5;
  border-radius: 12px;
  padding: 18px 20px;
  margin-bottom: 12px;
  flex: 1;
}
.ds-timeline-title {
  font-family: 'Syne', sans-serif;
  font-size: 0.95rem;
  font-weight: 700;
  color: #0A1628;
  margin-bottom: 6px;
}
.ds-timeline-desc {
  font-size: 0.85rem;
  color: #5a738a;
  line-height: 1.65;
  margin: 0;
}

/* ── FEATURES ─────────────────────────────────── */
.ds-features-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}
@media (max-width: 640px) {
  .ds-features-grid { grid-template-columns: 1fr; }
}
.ds-feature-card {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  background: #fff;
  border: 1.5px solid #e8eef5;
  border-radius: 10px;
  padding: 14px 16px;
}
.ds-feat-check {
  color: #00B4A0;
  font-weight: 700;
  font-size: 0.8rem;
  margin-top: 2px;
  flex-shrink: 0;
}
.ds-feat-text {
  font-size: 0.84rem;
  color: #3d5166;
  line-height: 1.5;
}

/* ── TOOLS ────────────────────────────────────── */
.ds-tools-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
}
@media (max-width: 640px) {
  .ds-tools-grid { grid-template-columns: repeat(2, 1fr); }
}
.ds-tool-card {
  display: flex;
  align-items: center;
  gap: 12px;
  background: #fff;
  border: 1.5px solid #e8eef5;
  border-radius: 12px;
  padding: 14px 16px;
  transition: border-color 0.2s, box-shadow 0.2s;
}
.ds-tool-card:hover {
  border-color: #00B4A0;
  box-shadow: 0 4px 16px rgba(0,180,160,0.1);
}
.ds-tool-logo-wrap {
  width: 48px;
  height: 48px;
  border-radius: 10px;
  border: 1px solid #e8eef5;
  background: #f8fbff;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.ds-tool-logo-wrap img {
  width: 40px;
  height: 40px;
  object-fit: contain;
}
.ds-tool-info {
  flex: 1;
  min-width: 0;
}
.ds-tool-name {
  display: block;
  font-size: 0.85rem;
  font-weight: 600;
  color: #0A1628;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.ds-tool-type {
  display: block;
  font-size: 0.72rem;
  color: #5a738a;
  text-transform: capitalize;
}

/* ── USE CASES ────────────────────────────────── */
.ds-usecases {
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.ds-usecase-card {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  background: #fff;
  border: 1.5px solid #e8eef5;
  border-radius: 10px;
  padding: 16px 18px;
}
.ds-uc-check {
  width: 24px;
  height: 24px;
  min-width: 24px;
  border-radius: 6px;
  background: rgba(0,180,160,0.1);
  color: #00B4A0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.72rem;
  font-weight: 700;
}
.ds-uc-text {
  font-size: 0.875rem;
  color: #3d5166;
  line-height: 1.55;
}

/* ── RELATED ──────────────────────────────────── */
.ds-related-grid {
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.ds-related-card {
  display: flex;
  align-items: center;
  gap: 14px;
  background: #fff;
  border: 1.5px solid #e8eef5;
  border-radius: 12px;
  padding: 16px 18px;
  color: inherit;
  transition: border-color 0.2s, transform 0.2s;
}
.ds-related-card:hover {
  border-color: #00B4A0;
  transform: translateX(4px);
}
.ds-related-icon {
  font-size: 1.5rem;
  padding: 8px;
  background: rgba(0,180,160,0.08);
  border-radius: 10px;
  flex-shrink: 0;
}
.ds-related-info { flex: 1; min-width: 0; }
.ds-related-name {
  display: block;
  font-size: 0.9rem;
  font-weight: 600;
  color: #0A1628;
}
.ds-related-desc {
  display: block;
  font-size: 0.78rem;
  color: #5a738a;
  margin-top: 2px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.ds-related-arrow {
  color: #00B4A0;
  font-size: 1rem;
  flex-shrink: 0;
}

/* ── SIDEBAR ──────────────────────────────────── */
.ds-sidebar {}
.ds-sidebar-sticky {
  position: sticky;
  top: 80px;
}
.ds-sidebar-banner {
  background: rgba(0,180,160,0.08);
  border: 1px solid rgba(0,180,160,0.2);
  border-radius: 10px;
  padding: 12px 16px;
  margin-bottom: 14px;
}
.ds-sidebar-banner p {
  font-size: 0.85rem;
  color: #0A1628;
  margin: 0;
  line-height: 1.5;
}
.ds-sidebar-banner strong { color: #00B4A0; }
.ds-contact-strip {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-top: 14px;
  padding: 14px 16px;
  background: #fff;
  border: 1.5px solid #e8eef5;
  border-radius: 12px;
}
.ds-contact-strip a {
  font-size: 0.85rem;
  color: #00B4A0;
  font-weight: 500;
  transition: color 0.2s;
}
.ds-contact-strip a:hover { color: #009d8c; }
</style>
